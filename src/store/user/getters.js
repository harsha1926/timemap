const uid = (state) => state.uid
const email = (state) => state.email
const displayName = (state) => state.displayName
const photoURL = (state) => state.photoURL

export default {
  uid,
  email,
  displayName,
  photoURL
}
