const countryCode = (state) => state.countryCode
const callingCode = (state) => state.callingCode
const phoneNumber = (state) => state.phoneNumber

export default {
  countryCode,
  callingCode,
  phoneNumber
}
